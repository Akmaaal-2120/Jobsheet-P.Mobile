if else dan switch case bisa digunakan untuk validasi 

perulangan ada 4 yaitu for loop, for in, while, do while



git clone = untuk cloning repo

git push origin <nama branch> = untuk push ke branch yang diinginkan

git pull origin <nama branch> = untuk pull ke branch yang diinginkan

git add file.dart = untuk menambah file ke staged change

git commit -m "pesan" = untuk commit

git branch <nama branch> = untuk menambah branch

git branch -d <nama branch> = hapus branch

git checkout <nama branch> = pindah branch

git checkout -b <nama branch> = buat dan pindah branch

git merge <nama branch> = merge branch (harus pindah ke branch tujuan dulu)



